package Arise.Utils;

import Arise.Base.BaseTest;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.ui.WebDriverWait;

import java.time.Duration;
import java.util.List;
import java.util.function.Function;

public class generic {
   // public static void waitForElement(List<WebElement> element) throws Exception {
     //   Function testConditionWait= i->element.size()>0;
      //  WebDriverWait wait=new WebDriverWait(BaseTest.driver, Duration.ofMillis(2));

  //     try
    //   {
   //     wait.until(testConditionWait);
 //   }catch(Exception e){
    //       throw new Exception("WebElement not visible after 2 min of wait time...!!");
   //    }

    //   }
   // public static void action_click(List<WebElement> elements) throws Exception {
    //    waitForElement(elements);
    //    elements.get(0).click();

   // }
  //  public static void  action_sendkeys(List<WebElement> elements,String value){
     //   elements.get(0).sendKeys(value);
   }
